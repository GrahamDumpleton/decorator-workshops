# Registering functions

Write a decorator that wraps nothing and changes nothing: it records the
function and hands the same object straight back. Build a command
dispatcher, a route table and an event system from that one shape, and
recognise it as what Flask, Click and pytest have been doing all along.

Fifteen minutes, in a notebook. Python and its standard library only,
nothing to install.
