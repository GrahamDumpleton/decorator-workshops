# What a decorator does

Meet decorators as a user before writing one. Put `@functools.cache`,
`@property` and `@dataclasses.dataclass` to work, watch each change what
a function or a class does, and find out what the `@` line actually
means.

Ten minutes, in a notebook. Python and its standard library only,
nothing to install.
