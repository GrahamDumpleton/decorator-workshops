# Your first decorator

Write the thing that goes after the `@`. Wrap a function by hand with
no `@` anywhere, then with the `@` syntax, and confirm the two are the
same. Then build a timer, a call counter, and a decorator that stops
the call happening at all.

Fifteen minutes, in a notebook. Python and its standard library only,
nothing to install.
